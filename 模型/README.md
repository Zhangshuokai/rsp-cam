# 模型

训练好的模型权重、序列化产物与推理配置。

## 约定

- **大文件不入库**：权重超过数 MB 时用 Git LFS 或外部存储，仓库内只留说明与下载方式。
- 每个模型记录：名称、来源、训练脚本（在 `训练/`）、输入尺寸、精度指标、日期。
- 命名建议：`<任务>-<网络/版本>-<日期>.<扩展名>`，例如 `sorting-yolo11n-20260925.onnx`。
