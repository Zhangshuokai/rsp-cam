# 教学demo

可直接运行的最小演示。每个 demo 一个子目录，内含该 demo 的 `README.md`（目的、依赖、运行方式）。

## 目录

- `摄像头远程显示/` —— 树莓派 USB 摄像头经裸 TCP 推流，本机 OpenCV 窗口实时显示。

## 约定

- 只放“能跑起来、能看懂”的最小示例，每个 demo 自包含。
- 生产/复用代码放 `算法/`（或后续的 `src/`），不要在 demo 里堆功能。
