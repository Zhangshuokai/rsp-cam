# 数据

数据集目录，遵循 Cookiecutter Data Science 约定：

| 子目录 | 用途 |
| --- | --- |
| `raw/` | 原始数据，只读不改 |
| `interim/` | 中间处理结果 |
| `processed/` | 建模用最终数据 |
| `external/` | 第三方数据 |

## 约定

- **数据内容不入库**（`.gitignore` 已忽略各子目录内容，仅保留 `.gitkeep` 占位）。
- 需要入库的小样本请在说明中注明来源与用途。
- 数据格式与处理流程写进 `训练/` 的对应脚本说明。
